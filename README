### 熟练掌握C的主要语法、网络请求



- 头文件
  - c文件、h文件、定义、声明（详解）：https://blog.csdn.net/Lambert_Zhaglog/article/details/83279363
  - 引用：
  头文件引用有两种形式：#include < stdio.h> 和 include "main.h “。前者引用系统头文件，后者引用自定义头文件；
  - 内容：
  头文件里一般包括宏定义， 全局变量， 函数原型声明，头文件。

- 结构体struct：https://blog.csdn.net/yanggangclcsdn/article/details/49718131
  - 定义
  - 作为参数、返回值、指针
  - 赋值：
    注意：不能直接给数组名赋值，因为数组名是一个常量，strcpy(stu1.name, "Jack");


- 方法重载
  用c语言实现函数重载：https://blog.csdn.net/ypshowm/article/details/89096042
  c语言中不允许有同名函数，因为编译时函数命名是一样的，不像c++会添加参数类型和返回类型作为函数编译后的名称，进而实现重载。
  如果要用c语言显现函数重载，可通过以下方式来实现：
  1、使用函数指针来实现，重载的函数不能使用同名称，只是类似的实现了函数重载功能
  2、重载函数使用可变参数，方式如打开文件open函数
  3、gcc有内置函数，程序使用编译函数可以实现函数重载


- 数据结构
  - 单链表反转：https://www.cnblogs.com/keeya/p/9218352.html
    1.递归法：从最后一个Node开始，在弹栈的过程中将指针顺序置换位置；
    2.遍历法：就是链表遍历的过程中将指针顺序置换



